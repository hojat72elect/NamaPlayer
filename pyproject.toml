[project]
name = "nama-player"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.3",
    "vulture>=2.16",
     "pytest>=9.1.1",
]

[tool.ruff]
line-length = 200
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "UP"]
